import { useState } from 'react';
import { clsx } from 'clsx';
import CityReactController from '@/components/CityReactController';

const CityReactPage = () => {
  return <CityReactController />;
};

export default CityReactPage;
